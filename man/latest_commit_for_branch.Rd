% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_metadata.R
\name{latest_commit_for_branch}
\alias{latest_commit_for_branch}
\title{Get the latest commit for a specific branch (internal)}
\usage{
latest_commit_for_branch(package, branch)
}
\arguments{
\item{package}{Character. Core PIP package name.}

\item{branch}{Character scalar. Branch name. No default; must be
provided explicitly by internal callers.}
}
\value{
A list (the raw GitHub API response) containing at minimum:
  `$sha` (the commit SHA) and `$commit$author` (with `$date` and `$name`).
  On error, returns a list with `$commit$author$date` = `NA` and
  `$commit$author$name` = `NA`.
}
\description{
Fetches the latest commit object for a given branch of a package from
the GitHub API. Returns a fallback list of `NA` values on error so
callers can handle missing branches gracefully.
}
\keyword{internal}
