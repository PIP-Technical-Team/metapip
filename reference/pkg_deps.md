# List all dependencies

List all dependencies

## Usage

``` r
pkg_deps(x = "metapip", recursive = FALSE)
```

## Arguments

- x:

  A character string for the packages being evaluated.

- recursive:

  If \`TRUE\`, will also list all dependencies of metapip packages.
