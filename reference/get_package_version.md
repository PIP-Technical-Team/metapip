# Get DESCRIPTION versions for all branches of each package (internal)

Get DESCRIPTION versions for all branches of each package (internal)

## Usage

``` r
get_package_version(package)
```

## Arguments

- package:

  Character vector of core PIP package names.

## Value

Named list where each element corresponds to a package and contains a
named character vector of branch -\> version mappings.
