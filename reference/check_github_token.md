# make sure your GITHUB credentials are properly setup

make sure your GITHUB credentials are properly setup

## Usage

``` r
check_github_token()
```

## Value

invisible TRUE if credentials are perfectly set

## Examples

``` r
if (FALSE) { # \dontrun{
check_github_token()
} # }
```
